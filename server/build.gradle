description = 'Server part of GYBoML project'
dependencies {
  compile project(':core')
}

jar {
    manifest {
        attributes "Main-Class": "ru.spbstu.gyboml.server.Main"
    }

    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
